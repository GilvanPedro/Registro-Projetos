# URL da API Java — usada tanto no server (SSR) quanto no client
NEXT_PUBLIC_API_URL=http://localhost:8080/api

# Credenciais simples do /admin (troque isso em producao por algo de verdade)
ADMIN_USER=admin
ADMIN_PASSWORD=troque-esta-senha
